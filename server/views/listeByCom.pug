extends layout

block content

  .container
    h1 test&nbsp;:
      each bur in listeBur
        .row
          .col-sm-6 #{bur.nomcom} - #{bur.bur}
          .col-sm-6
            a.btn.btn-primary.btn-block(href=`/bureau_vote/${bur.insee}/${bur.bur}` target="_blank") Voir plus
        p= bur.insee
        p= bur.bur
